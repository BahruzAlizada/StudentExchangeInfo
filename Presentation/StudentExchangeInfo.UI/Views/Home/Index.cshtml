@{
    ViewData["Title"] = "Ana Səhifə";
}

@section Css {
    <style>
        /* Style the buttons that are used to open and close the accordion panel */
        .accordion {
            background-color: white;
            color: black;
            cursor: pointer;
            padding: 18px;
            width: 100%;
            text-align: left;
            border-radius: 5px;
            border: 1px solid #dedede;
            outline: none;
            transition: all linear .15s;
            text-shadow: 0 1px 0 #444;
        }

            /* Add a background color to the button if it is clicked on (add the .active class with JS), and when you move the mouse over it (hover) */
            .active, .accordion:hover {
                background-color: #337ab7;
                color: white;
            }

        /* Style the accordion panel. Note: hidden by default */
        .panel {
            padding: 0 18px;
            background-color: white;
            display: none;
            overflow: hidden;
        }
    </style>
}

@{
    int number = 1;
}

@model HomeVM


<div class="slider-area">
	<div class="slider-active">
		@foreach (Slider item in Model.Sliders)
		{
            <div class="slider-item" style="background-image:url('/assets/img/slider/@item.Image');height:590px; background-repeat:no-repeat;background-size: cover; object-fit:cover;">
				<div class="overlay"></div>
				<div class="container">
					<div class="row no-gutters slider-text align-items-center justify-content-center">
						<div class="col-md-8 ftco-animate">
							<div class="text w-100 text-center" style="text-align:center; justify-content:center;align-items:center; margin-top:200px;">
								<h2 style="color:white; font-size:50px;">@item.Title</h2>
								<h1 class="mb-4" style="color:white;font-size:60px;">@item.SubTitle</h1>
							</div>
						</div>
					</div>
				</div>
			</div>
		}
	</div>
</div>



<!-- University Start -->
<div style="margin-top:170px;" class="popular-items section-padding20">
            <div class="container">
                <!-- Section tittle -->
                <div class="row justify-content-center">
                    <div class="col-xl-7 col-lg-8 col-md-10">
                        <div class="section-tittle mb-70 text-center">
                            <h2>Tərəfdaş Universitetlər</h2>
                            <p>Bizimlə əməkdaşlıq edən universitetlərin siyahısı aşağıdakılardır.</p>
                        </div>
                    </div>
                </div>
                <div class="row">
                    @foreach(University university in Model.Universities)
                    {
                        <div class="col-xl-3 col-lg-3 col-md-6 col-sm-6">
                        <div class="single-popular-items mb-50 text-center">
                            <div class="popular-img">
                                <img style="height:220px; width:220px;" src="~/assets/img//university/@university.Image" alt="">
                                @* <div class="img-cap">
                                    <span>Daha Ətraflı</span>
                                </div> *@
                            </div>
                            <div class="popular-caption">
                            <h5 style="color:black;">@university.Name</h5>
                            </div>
                        </div>
                    </div>
                    }
                </div>
                <!-- Button -->
                <div class="row justify-content-center" style="margin-top:-65px;">
                    <div class="room-btn pt-60">
                        <a asp-controller="University" asp-action="Index" class="btn view-btn1 text-white">Daha Çox</a>
                    </div>
                </div>
            </div>
        </div>
<!-- University End -->

<!-- About Start-->
<div class="about-details section-padding" style="margin-top:-15px;">
    <div class="container">
        <div class="row">
            <div class="offset-xl-1 col-lg-10">
                <div class="about-details-cap mb-50">
                    <h4>Haqqımızda</h4>
                    @Html.Raw(Model.About.Description)
                </div>
            </div>
        </div>
    </div>
</div>
<!-- About End-->

<!-- Blog Start -->
<div style="margin-top:-35px;" class="popular-items section-padding20">
    <div class="container">
        <!-- Section tittle -->
        <div class="row justify-content-center">
            <div class="col-xl-7 col-lg-8 col-md-10">
                <div class="section-tittle mb-70 text-center">
                    <h2>Xəbərlər</h2>
                    <p>Bizimlə gündəmdə qalın.</p>
                </div>
            </div>
        </div>
        <div class="row">
            @foreach (Blog blog in Model.Blogs)
            {
                <div class="col-xl-4 col-lg-4 col-md-6 col-sm-6">
                    <div class="single-popular-items mb-50 text-center">
                        <div class="popular-img">
                            <img style="height:250px; width:380px; object-fit:cover;" src="~/assets/img//blogs/@blog.Image" alt="">
                            <div class="img-cap">
                        <span>Daha Ətraflı</span>
                        </div>
                        </div>
                        <div class="popular-caption">
                            <h5 style="color:black;">@blog.Title</h5>
                        </div>
                    </div>
                </div>
            }
        </div>
        <!-- Button -->
@*         <div class="row justify-content-center" style="margin-top:-65px;">
            <div class="room-btn pt-60">
                <a asp-controller="University" asp-action="Index" class="btn view-btn1 text-white">Daha Çox</a>
            </div>
        </div> *@
    </div>
</div>
<!-- Blog End -->

@section Scripts {
    <script>
        var acc = document.getElementsByClassName("accordion");
        var i;

        for (i = 0; i < acc.length; i++) {
            acc[i].addEventListener("click", function () {
                /* Toggle between adding and removing the "active" class,
                to highlight the button that controls the panel */
                this.classList.toggle("active");

                /* Toggle between hiding and showing the active panel */
                var panel = this.nextElementSibling;
                if (panel.style.display === "block") { 
                    panel.style.display = "none";
                } else {
                    panel.style.display = "block";
                }
            });
        }
    </script> 
}
